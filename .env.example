# Frontend (Vite) — optional, defaults already work for local dev.
# VITE_API_BASE_URL=/api

# Backend (server/.env) — copy this file to server/.env and fill in your key.
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-sonnet-5
PORT=8787
